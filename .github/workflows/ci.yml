name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  merge_group:
    types: [checks_requested]
  release:
    types: [published]

permissions:
  contents: write
  pull-requests: write
  checks: write

jobs:
  #region Linting and formatting
  # =============================================================================
  # Linting and formatting checks
  # =============================================================================

  ruff-format:
    name: Ruff format
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install python dependencies
        run: uv sync --locked --all-extras --dev

      - name: Setup reviewdog
        if: github.event_name == 'pull_request'
        uses: reviewdog/action-setup@d8a7baabd7f3e8544ee4dbde3ee41d0011c3a93f # v1.5.0
        with:
          reviewdog_version: v0.20.3

      - name: Ruff format (reviewdog annotations)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          uv run ruff format --diff \
            | reviewdog -f=diff -name="ruff format" -reporter=github-pr-review -filter-mode=nofilter -fail-level=any

      - name: Ruff format
        run: python3 tools/check.py ruff-format

  ruff-lint:
    name: Ruff lint
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install python dependencies
        run: uv sync --locked --all-extras --dev

      - name: Setup reviewdog
        if: github.event_name == 'pull_request'
        uses: reviewdog/action-setup@d8a7baabd7f3e8544ee4dbde3ee41d0011c3a93f # v1.5.0
        with:
          reviewdog_version: v0.20.3

      - name: Ruff lint (reviewdog annotations)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          uv run ruff check --output-format rdjson \
            | reviewdog -f=rdjson -name="ruff lint" -reporter=github-pr-review -filter-mode=nofilter -fail-level=any

      - name: Ruff lint
        run: python3 tools/check.py ruff-lint

  pyright:
    name: Pyright
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install python dependencies
        run: uv sync --locked --all-extras --dev

      - name: Setup reviewdog
        if: github.event_name == 'pull_request'
        uses: reviewdog/action-setup@d8a7baabd7f3e8544ee4dbde3ee41d0011c3a93f # v1.5.0
        with:
          reviewdog_version: v0.20.3

      - name: Pyright (reviewdog annotations)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          # Run pyright with JSON output, capturing regardless of exit code
          uv run pyright --outputjson > pyright_output.json || true
          # Convert to rdjson format and pipe to reviewdog
          jq '{
            source: {name: "pyright", url: "https://github.com/Microsoft/pyright"},
            severity: "ERROR",
            diagnostics: [.generalDiagnostics[]? | {
              message: (if .rule then "\(.message) (\(.rule))" else .message end),
              severity: (.severity | ascii_upcase),
              location: {
                path: .file,
                range: {
                  start: {line: (.range.start.line + 1), column: (.range.start.character + 1)},
                  end: {line: (.range.end.line + 1), column: (.range.end.character + 1)}
                }
              }
            }]
          }' pyright_output.json \
            | reviewdog -f=rdjson -name="pyright" -reporter=github-pr-review -filter-mode=nofilter -fail-level=any

      - name: Pyright
        run: python3 tools/check.py pyright

  prettier:
    name: Prettier
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0

      - name: Setup reviewdog
        if: github.event_name == 'pull_request'
        uses: reviewdog/action-setup@d8a7baabd7f3e8544ee4dbde3ee41d0011c3a93f # v1.5.0
        with:
          reviewdog_version: v0.20.3

      - name: Prettier (reviewdog annotations)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          npm ci
          npx --yes prettier@3 --write .
          git diff \
            | reviewdog -f=diff -name="prettier" -reporter=github-pr-review -filter-mode=nofilter -fail-level=any

      - name: Prettier
        run: python3 tools/check.py prettier

  mdformat:
    name: Markdown format
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install python dependencies
        run: uv sync --locked --all-extras --dev

      - name: Setup reviewdog
        if: github.event_name == 'pull_request'
        uses: reviewdog/action-setup@d8a7baabd7f3e8544ee4dbde3ee41d0011c3a93f # v1.5.0
        with:
          reviewdog_version: v0.20.3

      - name: Markdown format (reviewdog annotations)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          uv run mdformat $(git ls-files '*.md' '*.mdx')
          git diff \
            | reviewdog -f=diff -name="mdformat" -reporter=github-pr-review -filter-mode=nofilter -fail-level=any

      - name: Markdown format
        run: python3 tools/check.py mdformat

  import-linter:
    name: Import boundaries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install python dependencies
        run: uv sync --locked --all-extras --dev

      - name: Check import boundaries
        run: python3 tools/check.py imports

  frontend-card-check:
    name: Frontend card checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Node
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: 22

      - name: Run frontend card quality checks
        run: python3 tools/check.py frontend

      - name: Upload frontend coverage to Codecov
        # Codecov uploads are tokenless, which only works for the repo registered
        # on Codecov. A fork has no token and the upload is rejected, so skip it
        # there for the same reason the HACS job is skipped. Drop the repository
        # test if this fork is ever registered and given a CODECOV_TOKEN.
        if: ${{ github.event_name != 'release' && !cancelled() && github.repository == 'hass-energy/haeo' }}
        uses: codecov/codecov-action@e53489f4d376d79066609109e7a95a29eb3740b1 # v7.0.0
        with:
          files: frontend/haeo-forecast-card/coverage/lcov.info
          flags: frontend
  #endregion
  #region Tests
  # =============================================================================
  # Tests
  # =============================================================================

  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install the project
        run: uv sync --locked --all-extras --dev

      - name: Run Tests
        run: python3 tools/check.py test -- --cov=custom_components/haeo --cov-branch --cov-report=xml --junitxml=junit.xml -o junit_family=legacy

      - name: Upload test results to Codecov
        # Skipped on forks — see the frontend coverage upload above.
        if: ${{ github.event_name != 'release' && !cancelled() && github.repository == 'hass-energy/haeo' }}
        uses: codecov/codecov-action@e53489f4d376d79066609109e7a95a29eb3740b1 # v7.0.0
        with:
          report_type: test_results

      - name: Upload coverage to Codecov
        # Skipped on forks — see the frontend coverage upload above.
        if: ${{ github.event_name != 'release' && !cancelled() && github.repository == 'hass-energy/haeo' }}
        uses: codecov/codecov-action@e53489f4d376d79066609109e7a95a29eb3740b1 # v7.0.0
        with:
          flags: python
          fail_ci_if_error: true

  scenario:
    name: Scenario tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install uv
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install the project
        run: uv sync --locked --all-extras --dev

      - name: Set up Node.js
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: 22

      - name: Install card dependencies
        run: npm --prefix frontend/haeo-forecast-card ci

      - name: Build topology renderer
        run: npm --prefix frontend/haeo-forecast-card run build

      - name: Run scenario tests
        run: python3 tools/check.py scenario -- -vv
  #endregion

  #region Validation
  # =============================================================================
  # Validation checks
  # =============================================================================

  hassfest:
    name: Hassfest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run hassfest validation
        uses: home-assistant/actions/hassfest@87c064c607f3c5cc673a24258d0c98d23033bfc3 # master

  version-check:
    name: Version consistency
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install uv
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Check version consistency
        run: python3 tools/check.py version

  hacs:
    name: HACS
    runs-on: ubuntu-latest
    # HACS publish requirements (issues enabled, repo topics, etc.) only apply
    # to the canonical integration repo. Skip on forks so they don't show red.
    if: github.repository == 'hass-energy/haeo'
    steps:
      - name: Run HACS validation
        uses: hacs/action@d556e736723344f83838d08488c983a15381059a # 22.5.0
        with:
          category: integration
          # Remove this 'ignore' key when you have added brand images for your integration to https://github.com/home-assistant/brands
          ignore: brands

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0

      - name: Install dependencies
        run: uv sync

      - name: Build documentation
        run: python3 tools/check.py docs
  #endregion

  #region CI passed
  # =============================================================================
  # CI passed (single required check)
  # =============================================================================

  ci-passed:
    name: CI passed
    if: always()
    needs:
      - ruff-format
      - ruff-lint
      - pyright
      - prettier
      - mdformat
      - import-linter
      - frontend-card-check
      - test
      - scenario
      - hassfest
      - version-check
      - hacs
      - docs
    runs-on: ubuntu-latest
    steps:
      - name: Check all jobs passed
        run: |
          # 'skipped' is allowed (e.g., hacs is gated to the upstream repo).
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]]; then
            echo "::error::One or more required jobs failed"
            exit 1
          fi
          if [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "::error::One or more required jobs were cancelled"
            exit 1
          fi
          echo "All required jobs passed!"
  #endregion
